<%= form_for [@driver, @review] do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %><br>

    <%= f.label :content, "Review" %>
    <%= f.text_area :content %>
    <%= f.hidden_field "user_id", value: current_user.id %>
    <%= f.submit %>
<% end %>